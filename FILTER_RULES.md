# マウント・ミニオン追加フィルターのデータ判断

FFXIV Collectの日本語マスターを確認した。マウント353件、ミニオン583件の各レコードには `tradeable` と `sources` がある。`sources` のフィールドは `type`、`text`、`related_type`、`related_id` だけであり、現在入手可能かどうかを直接示す日時または状態フラグは存在しない。

| 条件 | 判定方法 | 表示上の表現 |
|---|---|---|
| マーケットボード購入可 | `tradeable === true` | MB購入可／MB購入不可 |
| 課金要素 | `sources.type === "Premium"` | 課金・キャンペーン系あり／なし |
| 現在入手可能性 | MB購入可、恒常コンテンツ由来、またはオンラインストア・コレクターズエディション由来を「通常入手可」とする。イベント、期間キャンペーン、PvPシリーズ、レガシー等のみのものは「期間限定・要確認」とする。 | 通常入手可／期間限定・要確認 |

> FFXIV Collectのソースにはライブの開催期間や終了状態がないため、「イベントがまさに開催中か」「キャンペーンが当月だけ実施中か」をAPIだけで断定することはできない。そこで、誤って入手可と表示しないよう、期間依存の取得手段は **期間限定・要確認** に分類する。マーケットボードで取引できるものは、イベント終了後でも入手手段が存在するため通常入手可に含める。
